{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cargobuild",
            "type": "shell",
            "command": "cd backend && cargo build",
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
